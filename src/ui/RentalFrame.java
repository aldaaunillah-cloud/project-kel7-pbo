package ui;

import javax.swing.*;

public class RentalFrame extends JFrame {
    public RentalFrame() {
        setTitle("Rental - Rental PS");
        setSize(900, 600);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(JFrame.DISPOSE_ON_CLOSE);
        // TODO
    }
}
