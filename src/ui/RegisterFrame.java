package ui;

import javax.swing.*;

public class RegisterFrame extends JFrame {

    public RegisterFrame() {
        setTitle("Register");
        setSize(300, 200);
        setLocationRelativeTo(null);
        setVisible(true);
    }
}
